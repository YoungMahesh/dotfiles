# append this line to os generated .bashrc file: source ~/.bashrc-1

# ------------------ fzf -----------------------
fz() {
    local dirs
    dirs=$(find ~/company ~/personal -maxdepth 1 | fzf)
    
    if [ -n "$dirs" ]; then
        cd "$dirs" && clear
    fi
}


# ----------------- history ---------------------------
# Set maximum number of commands in memory during a session
HISTSIZE=500
# Set maximum number of commands to save in the history file
HISTFILESIZE=500
# Optional: Ignore duplicate commands and commands starting with space
HISTCONTROL=ignoreboth
# Bind Ctrl+R to fzf history search
__fzf_history__() {
    local selected
    selected=$(history | fzf --tac --no-sort --exact +m -e | awk '{$1=""; print $0}' | sed 's/^ *//')
    
    if [ -n "$selected" ]; then
        READLINE_LINE="$selected"
        READLINE_POINT=${#READLINE_LINE}
    fi
}
# Bind the function to Ctrl+R
bind -x '"\C-r": __fzf_history__'


# ------------------- git --------------------
alias gs='git status'
#alias gd='git diff'
#alias gdc='git diff --cached'
alias ga='git add .'
alias gl='git log'
alias gc='git commit -m'
alias gp='git push'


# ---------------------- package installations ------------------------------
# bun
export BUN_INSTALL="$HOME/.bun"
export PATH=$BUN_INSTALL/bin:$PATH

# if you have installed vlc through flathub
#alias vlc='flatpak run org.videolan.VLC'

# golang path
#export PATH=$PATH:/usr/local/go/bin
#export PATH=${PATH}:`go env GOPATH`/bin

# path for https://github.com/sharkdp/fd 
#export PATH=$PATH:$HOME/.local/bin

# folder color to orange, no need in alacritty
#export LS_COLORS="$LS_COLORS:di=38;5;208"

# for ansible
#export LC_ALL="en_US.UTF-8"
#export LANG="en_US.UTF-8"

# pnpm
#export PNPM_HOME="/home/mahesh/.local/share/pnpm"
#case ":$PATH:" in
#  *":$PNPM_HOME:"*) ;;
#  *) export PATH="$PNPM_HOME:$PATH" ;;
#esac
#alias pn='pnpm'

# cli through docker, --network host tells cli to look network on host computer instead of inside docker-container
#alias stripe='docker run --rm -it --network host stripe/stripe-cli:latest'


export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

export EDITOR=nvim
export PATH="$PATH:/opt/nvim-linux64/bin"
alias vi=nvim

alias python=python3
